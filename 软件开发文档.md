# 软件开发文档

## 单细胞 ANN 检索系统

**项目名称**：基于 HNSWLIB 的单细胞近似最近邻检索系统

**GitHub 仓库**：https://github.com/zhy1057/2026-

**文档版本**：v1.0

---

## 一、项目概述

### 1.1 开发背景

单细胞测序技术（scRNA-seq）近年来飞速发展，能够在单个细胞水平上获取基因表达谱信息，为疾病诊断、药物研发、细胞类型鉴定等领域提供了前所未有的数据粒度。以人体肝脏数据集为例，单个数据集即包含约 69,000 个细胞、32,000+ 个基因的高维表达矩阵。

然而，随着数据规模的急剧增长，如何从海量单细胞数据中快速检索与目标细胞相似的细胞群体，已成为生物信息学分析中的核心挑战。传统的暴力精确检索（线性扫描）在大规模数据上耗时显著，无法满足交互式分析的需求。

近似最近邻（Approximate Nearest Neighbor, ANN）算法通过在精度与速度之间取得平衡，可将查询时间从毫秒级压缩至亚毫秒级。HNSW（Hierarchical Navigable Small World）是目前综合性能最优的 ANN 算法之一，HNSWLIB 是其成熟的开源实现。

基于上述背景，本项目旨在开发一套面向单细胞高维向量数据的 ANN Web 检索系统，提供数据导入、索引构建、相似细胞检索、条件筛选、交互式可视化及用户管理等完整功能。

### 1.2 项目目标

| 目标类别 | 具体目标 |
|---------|---------|
| **功能目标** | 支持 .h5ad 数据集导入、HNSW 索引构建、Top-K 相似细胞检索、条件过滤、UMAP/t-SNE 可视化、用户认证与管理 |
| **性能目标** | ANN 单次查询 ≤ 1ms，相比暴力检索加速 100 倍以上，召回率 ≥ 95% |
| **可用性目标** | 响应式 Web 界面，操作直观，支持拖拽导入、缩放交互、一键联动 |
| **安全目标** | 密码加密存储，JWT 认证，权限分级（普通用户/管理员） |

### 1.3 开发环境

| 项目 | 版本/工具 |
|------|----------|
| 操作系统 | Windows 11 |
| Python | 3.10+ |
| Node.js | 18+ |
| 前端框架 | Vue 3.5 + Vite 6 |
| 后端框架 | Flask 3.1 |
| ANN 引擎 | HNSWLIB 0.8+ |
| 数据读取 | scanpy 1.10+ |
| 可视化 | ECharts 5.5 |
| IDE | VS Code / Cursor |
| 版本管理 | Git + GitHub |

### 1.4 可行性分析

**技术可行性**：HNSWLIB 为成熟的开源 ANN 库，scanpy 是单细胞分析领域的事实标准，Vue 3 + Flask 为成熟的全栈组合，技术路线经过验证。

**经济可行性**：所有技术组件均为开源免费，无需额外软件授权费用。

**操作可行性**：系统采用 Web 界面，用户通过浏览器即可使用，无需安装额外客户端软件。

### 1.5 项目计划

| 阶段 | 内容 | 预计耗时 |
|------|------|---------|
| 第一阶段 | 项目初始化、环境搭建、技术调研 | 2 天 |
| 第二阶段 | 后端数据管理模块（数据集读取、存储、API） | 3 天 |
| 第三阶段 | 后端 HNSW 索引模块（构建、加载、参数调整） | 3 天 |
| 第四阶段 | 后端检索模块（Top-K、条件检索、召回率评测） | 3 天 |
| 第五阶段 | 后端用户系统（JWT 认证、权限管理） | 2 天 |
| 第六阶段 | 前端基础框架与路由 | 2 天 |
| 第七阶段 | 前端检索页面 | 3 天 |
| 第八阶段 | 前端可视化模块（UMAP/t-SNE 散点图） | 3 天 |
| 第九阶段 | 前端数据管理页面 | 2 天 |
| 第十阶段 | 性能评估模块与联调测试 | 3 天 |
| 第十一阶段 | 文档编写与部署 | 2 天 |

---

## 二、需求分析与系统设计

### 2.1 需求分析

#### 2.1.1 功能性需求

**数据管理**
- 支持导入本地 .h5ad 格式单细胞数据集
- 支持数据集列表查看、详情查看、删除
- 支持 HNSW 索引的构建、状态查询、参数调整（ef）、删除

**相似细胞检索**
- 支持以细胞编号作为查询，检索 Top-K 相似细胞
- 支持以自定义 30 维向量作为查询
- 支持精确检索（暴力扫描）和近似检索（ANN）两种模式切换
- 支持按细胞类型、组织来源等元数据进行条件过滤
- 显示查询耗时、距离、相似度等指标

**可视化分析**
- 支持 UMAP 和 t-SNE 两种降维嵌入切换
- 支持按细胞类型、捐赠者、性别等字段着色
- 支持大数据集自动采样（2K~30K 点）
- 支持检索结果在散点图上高亮标注
- 支持缩放、平移、分类显隐、导出图片等交互

**用户管理**
- 支持用户注册、登录、修改密码、注销
- 支持管理员创建/删除用户、设置角色权限
- 支持 JWT 令牌认证与路由守卫

#### 2.1.2 非功能性需求

- **性能**：ANN 检索响应时间 < 1ms（6.9 万细胞数据集），加速比 > 100x
- **可靠性**：系统稳定运行，异常操作有明确错误提示
- **安全性**：密码采用 PBKDF2-SHA256 加密存储，JWT 令牌 24 小时过期
- **易用性**：界面简洁直观，支持拖拽导入、表单校验、操作反馈

### 2.2 系统设计

#### 2.2.1 总体架构

系统采用 B/S（浏览器/服务器）架构，前后端分离设计：

```
┌─────────────────────────────────────────────────────┐
│                    浏览器（用户）                      │
│          Vue 3 + Vite + ECharts + Axios              │
└────────────────────────┬────────────────────────────┘
                         │ HTTP / JSON (端口 3000)
                         ▼
┌─────────────────────────────────────────────────────┐
│                   前端开发服务器                       │
│              Vite Dev Server (端口 3000)              │
│           /api/* 请求代理至后端 (端口 5000)            │
└────────────────────────┬────────────────────────────┘
                         │ REST API
                         ▼
┌─────────────────────────────────────────────────────┐
│                    后端 Flask 服务                     │
│                 (端口 5000, Blueprint)                │
│                                                     │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌────────┐ │
│  │ 数据管理  │ │ 索引管理  │ │ 检索服务  │ │ 用户服务│ │
│  └──────────┘ └──────────┘ └──────────┘ └────────┘ │
│                                                     │
│  ┌────────────────────────────────────────────────┐ │
│  │              HNSWLIB 引擎                       │ │
│  └────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────┐
│                    数据存储层                          │
│   .h5ad 文件  |  .bin 索引文件  |  JSON 元数据        │
└─────────────────────────────────────────────────────┘
```

#### 2.2.2 模块划分

| 模块 | 后端文件 | 功能 |
|------|---------|------|
| 数据管理 | `data_service.py`, `routes/data.py` | 数据集加载、导入、删除、元信息维护 |
| 索引管理 | `index_service.py`, `routes/search.py` | HNSW 索引构建/加载/删除/参数调整 |
| 检索服务 | `search_service.py`, `routes/search.py` | Top-K 检索、条件过滤、召回率评测 |
| 用户系统 | `user_service.py`, `routes/auth.py` | 注册/登录/认证/权限管理 |
| 可视化 | `visual_service.py`, `routes/visual.py` | UMAP/t-SNE 坐标提取、采样、高亮 |
| 前端核心 | `router/index.js`, `api/index.js`, `store/user.js` | 路由守卫、API 封装、状态管理 |
| 前端页面 | `views/*.vue` | 6 个功能页面 |

#### 2.2.3 关键技术决策

| 决策点 | 选型 | 理由 |
|--------|------|------|
| ANN 算法 | HNSWLIB (HNSW) | 查询速度最快、内存占用可控、支持动态更新 ef |
| 距离度量 | L2 (欧氏距离) | PCA 向量空间中 L2 距离等价于欧氏距离，语义清晰 |
| 前端框架 | Vue 3 Composition API | 轻量、响应式、组件化开发 |
| 状态管理 | Vue 3 reactive + localStorage | 无需引入 Pinia/Vuex，项目规模适中 |
| 可视化库 | ECharts | 支持大数据量渲染（large + progressive）、交互丰富 |
| 密码存储 | PBKDF2-SHA256 (100K 迭代) | 业界标准，无需额外依赖 |
| 认证方式 | JWT (HS256, 24h) | 无状态、前后端解耦、易于扩展 |

### 2.3 详细设计

#### 2.3.1 数据流设计

**检索流程**：
```
用户输入 (cell_index/vector)
    │
    ▼
检查是否有索引
    │
    ├── 有索引 → HNSW 近似检索 (O(log N))
    │              │
    │              ▼
    │           取 fetch_k 个候选
    │              │
    │              ▼
    │           条件过滤 (cell_type 等)
    │              │
    │              ▼
    │           不足 k 个？→ 退化到子集精确检索
    │
    └── 无索引 → 暴力精确检索 (O(N))
                  │
                  ▼
               条件过滤
    │
    ▼
返回 Top-K 结果 (cell_index, distance, similarity, metadata, umap/tsne)
```

**索引构建流程**：
```
管理员触发构建 (space, M, ef_construction, ef)
    │
    ▼
从 .h5ad 加载 PCA 向量 (n_cells × pca_dim)
    │
    ▼
创建 HNSW 索引
    │
    ▼
添加所有向量到索引
    │
    ▼
保存索引文件 (.bin) + 元数据 (JSON)
```

#### 2.3.2 核心算法

**HNSW 检索**：
- 基于分层可导航小世界图
- 查询时从最高层开始，逐层向下收缩搜索范围
- 时间复杂度 O(log N)，空间复杂度 O(N·M·L)
- ef 参数控制搜索宽度：ef 越大召回率越高，速度越慢

**相似度计算**：
```
distance = L2(query, target)
similarity = 1 / (1 + distance)   # 距离越小相似度越高
```

**条件检索智能退化策略**：
```python
fetch_k = min(k * 20, total_n)       # 初始扩大范围
for attempt in range(6):
    candidates = ann_search(fetch_k)
    filtered = filter_by_condition(candidates)
    if len(filtered) >= k: break
    fetch_k = min(fetch_k * 4, total_n)

if len(filtered) < k:
    # 退化到子集精确检索，确保返回 k 个结果
    valid_indices = get_all_matching_indices(condition)
    exact_search_on_subset(valid_indices, query, k)
```

### 2.4 数据库设计

本项目采用轻量级存储方案，不使用传统关系型数据库，而是使用 JSON 文件存储元数据：

#### 2.4.1 用户数据 (`users.json`)

```json
{
  "admin": {
    "username": "admin",
    "password_hash": "pbkdf2:sha256:100000$...",
    "salt": "a1b2c3d4...",
    "role": "admin",
    "created_at": "2025-06-01T10:00:00"
  }
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| username | string | 用户名（主键） |
| password_hash | string | PBKDF2-SHA256 哈希 |
| salt | string | 16 字节随机盐 |
| role | string | 角色：admin / user |
| created_at | string | 创建时间（ISO 格式） |

#### 2.4.2 数据集元数据 (`datasets_meta.json`)

```json
{
  "liver": {
    "name": "liver",
    "filename": "liver.h5ad",
    "n_cells": 69032,
    "n_genes": 32397,
    "pca_dim": 30,
    "cell_types": ["hepatocyte", "T cell", ...],
    "has_index": true,
    "created_at": "2025-06-01T10:00:00"
  }
}
```

#### 2.4.3 索引元数据 (`indexes_meta.json`)

```json
{
  "liver": {
    "dataset_id": "liver",
    "space": "l2",
    "M": 16,
    "ef_construction": 200,
    "ef": 50,
    "dim": 30,
    "num_elements": 69032,
    "file_size": 18538496,
    "created_at": "2025-06-01T12:00:00"
  }
}
```

### 2.5 UI 设计

#### 2.5.1 页面结构

系统共包含 6 个功能页面 + 2 个辅助页面：

| 页面 | 路由 | 权限 | 功能 |
|------|------|------|------|
| 首页 | `/` | 公开 | 系统概览、功能介绍、快捷导航 |
| 登录 | `/login` | 公开 | 用户认证 |
| 注册 | `/register` | 公开 | 新用户注册 |
| 相似检索 | `/search` | 登录 | 核心检索功能 |
| 可视化 | `/visual` | 登录 | UMAP/t-SNE 散点图 |
| 数据管理 | `/data` | 登录 | 数据集与索引管理 |
| 系统管理 | `/admin` | 管理员 | 用户账号管理 |
| 404 | `/*` | 公开 | 页面不存在提示 |

#### 2.5.2 核心页面布局

**检索页面（Search）**：左右两栏布局
- 左栏（360px）：参数配置面板（数据集、查询方式、Top-K、过滤条件）
- 右栏（自适应）：结果展示（元信息卡片 + 结果表格 + 详情弹窗）

**可视化页面（Visual）**：上下布局
- 上部：控制面板（数据集、嵌入类型、采样数、着色字段）+ 高亮输入
- 下部：ECharts 散点图（640px 高度）

**数据管理页面（DataManage）**：左右两栏布局
- 左栏（360px）：数据集列表（卡片式）
- 右栏（自适应）：详情卡片 + 索引管理卡片

#### 2.5.3 设计规范

- 主色调：蓝色系（#409eff）
- 状态色：成功 #67c23a、警告 #e6a23c、危险 #f56c6c
- 圆角：卡片 8px、按钮 4px、标签 10px
- 间距：卡片内边距 24px、模块间距 16px
- 响应式：768px 以下自动切换为单列布局

---

## 三、系统测试

### 3.1 测试环境

| 项目 | 配置 |
|------|------|
| 操作系统 | Windows 11 25H2 |
| CPU | Intel Core i7 |
| 内存 | 16 GB |
| Python | 3.10 |
| Node.js | 18 LTS |
| 测试数据集 | liver.h5ad (69,032 cells × 32,397 genes) |

### 3.2 功能测试

#### 3.2.1 用户系统测试

| 测试用例 | 预期结果 | 实际结果 |
|---------|---------|---------|
| 使用 admin/admin123 登录 | 登录成功，跳转首页 | 通过 |
| 错误密码登录 | 提示"用户名或密码错误" | 通过 |
| 注册重复用户名 | 提示"用户名已存在" | 通过 |
| 密码少于 6 位注册 | 提示"密码长度至少 6 位" | 通过 |
| 未登录访问 /search | 跳转 /login | 通过 |
| 普通用户访问 /admin | 提示"需要管理员权限" | 通过 |
| 修改密码 | 旧密码验证成功后更新 | 通过 |
| 管理员删除用户 | 删除成功，列表刷新 | 通过 |

#### 3.2.2 数据管理测试

| 测试用例 | 预期结果 | 实际结果 |
|---------|---------|---------|
| 导入 liver.h5ad | 显示进度，导入成功 | 通过 |
| 拖拽 .txt 文件 | 提示"仅支持 .h5ad 格式" | 通过 |
| 查看数据集详情 | 显示 cells/genes/PCA/obs 列 | 通过 |
| 构建 HNSW 索引 | 约 15 秒完成，文件大小 ~17MB | 通过 |
| 调整 ef 参数 | ef 更新成功 | 通过 |
| 删除索引 | 删除成功，列表更新 | 通过 |
| 删除数据集 | 连带删除索引文件 | 通过 |

#### 3.2.3 检索功能测试

| 测试用例 | 预期结果 | 实际结果 |
|---------|---------|---------|
| cell_index=100, k=10, ANN | 返回 10 个结果，耗时 < 1ms | 通过 |
| cell_index=100, k=10, 精确 | 返回 10 个结果，耗时 ~16ms | 通过 |
| 条件过滤 cell_type=T cell | 返回结果均为 T cell | 通过 |
| 自定义向量查询 | 返回最近邻细胞 | 通过 |
| 检索结果跳转可视化 | 自动高亮 Top-K 细胞 | 通过 |

#### 3.2.4 可视化测试

| 测试用例 | 预期结果 | 实际结果 |
|---------|---------|---------|
| 加载 UMAP 坐标 | 渲染散点图，19 类着色 | 通过 |
| 切换 t-SNE | 重新加载坐标 | 通过 |
| 修改采样点数 | 点数更新，渲染流畅 | 通过 |
| 切换着色字段 | 颜色按新字段分组 | 通过 |
| 高亮检索结果 | 红色 pin 置顶显示 | 通过 |
| 缩放/平移 | 交互流畅 | 通过 |

### 3.3 性能测试

#### 3.3.1 检索性能对比

测试条件：liver 数据集（69,032 cells × 30 PCA），k=10，50 次随机查询取平均

| 指标 | ANN 检索 | 暴力检索 | 加速比 |
|------|---------|---------|--------|
| 平均查询时间 | **0.145 ms** | 16.12 ms | **111x** |
| 索引构建时间 | 14.40 s | - | - |
| 索引文件大小 | 17.68 MB | - | - |
| 召回率 (ef=50) | **100%** | 100% (基准) | - |

#### 3.3.2 可视化性能

| 采样点数 | 首次渲染 | 交互帧率 |
|---------|---------|---------|
| 2,000 | < 1s | 60 fps |
| 8,000 | ~1s | 60 fps |
| 15,000 | ~2s | 30-60 fps |
| 30,000 | ~3s | 20-30 fps |

#### 3.3.3 前端构建产物

| 资源 | 大小 | Gzip |
|------|------|------|
| index (vendor) | 143 KB | 55 KB |
| Search 页面 | 10.87 KB | 4.10 KB |
| DataManage 页面 | 14.62 KB | 5.26 KB |
| Visual 页面 (含 ECharts) | 1.04 MB | 346 KB |

---

## 四、项目管理

### 4.1 参与人员及分工

| 角色 | 姓名 | 职责 |
|------|------|------|
| 项目负责人 | 张宇 | 整体架构设计、后端开发、性能优化 |
| 前端开发 | 张宇 | Vue 页面开发、可视化实现、UI 设计 |
| 后端开发 | 张宇 | API 开发、数据管理、用户系统 |
| 测试 | 张宇 | 功能测试、性能测试、文档编写 |

> （请根据实际团队成员填写）

### 4.2 项目进展记录

| 日期 | 里程碑 | 备注 |
|------|--------|------|
| 2025-06-01 | 项目初始化，技术选型确定 | Vue 3 + Flask + HNSWLIB |
| 2025-06-03 | 后端数据管理模块完成 | 支持 .h5ad 读取、元信息维护 |
| 2025-06-05 | 后端索引模块完成 | HNSW 构建/加载/删除 |
| 2025-06-07 | 后端检索模块完成 | Top-K、条件检索、召回率评测 |
| 2025-06-09 | 后端用户系统完成 | JWT + PBKDF2 + 权限管理 |
| 2025-06-11 | 前端基础框架完成 | 路由、API、状态管理 |
| 2025-06-13 | 前端检索页面完成 | 参数配置、结果展示、详情弹窗 |
| 2025-06-15 | 前端可视化完成 | ECharts 散点图、高亮联动 |
| 2025-06-17 | 前端数据管理完成 | 导入、索引管理、权限控制 |
| 2025-06-19 | 联调测试完成 | 全链路测试通过 |
| 2025-06-20 | 文档编写、项目提交 | README、开发文档 |

### 4.3 项目管理工具

| 工具 | 用途 |
|------|------|
| Git | 版本控制 |
| GitHub | 代码托管、协作开发 |
| VS Code | 代码编辑器 |
| Postman / cURL | API 调试 |
| Vite Dev Server | 前端开发服务器、热更新 |

---

## 五、用户手册

### 5.1 系统访问

在浏览器中访问 `http://localhost:3000` 即可打开系统首页。

> 确保后端（端口 5000）和前端（端口 3000）均已启动。详见项目 README.md。

### 5.2 账号登录

1. 点击右上角「登录」按钮进入登录页面
2. 输入用户名和密码
3. 点击「登录」按钮
4. 登录成功后自动跳转到首页

**默认管理员账号**：
- 用户名：`admin`
- 密码：`admin123`

### 5.3 导入数据集

1. 进入「数据管理」页面
2. 点击右上角「导入数据集」按钮
3. 在弹窗中拖拽或点击选择本地 .h5ad 文件
4. 可选填写数据集名称（留空则使用文件名）
5. 点击「开始导入」，等待进度条完成

### 5.4 构建索引

1. 在「数据管理」页面左侧选择数据集
2. 右侧「索引管理」区域，配置参数：
   - **space**：距离类型（L2 / cosine / ip）
   - **M**：双向连接数（默认 16）
   - **ef_construction**：构建质量（默认 200）
   - **ef**：查询时参数（默认 50）
3. 点击「开始构建」，等待构建完成（约 15 秒）

### 5.5 相似细胞检索

1. 进入「相似检索」页面
2. 选择已建索引的数据集
3. 选择查询方式：
   - **细胞编号**：输入 0 ~ n_cells-1 的整数，或点击「随机」
   - **自定义向量**：输入 30 维浮点数（逗号或空格分隔）
4. 调整 Top-K 值（1~100）
5. （可选）添加过滤条件（最多 3 个）
6. 点击「开始检索」
7. 查看结果表格，点击行查看详细 metadata
8. 点击「在可视化中查看」跳转到散点图

### 5.6 可视化分析

1. 进入「可视化」页面
2. 选择数据集
3. 切换 UMAP / t-SNE 嵌入
4. 调整采样点数（2K~30K）
5. 选择着色字段（cell_type / donor_id / sex 等）
6. 使用鼠标滚轮缩放、拖拽平移
7. 在「高亮检索结果」输入框输入细胞编号，查看高亮效果

### 5.7 管理用户（仅管理员）

1. 进入「系统管理」页面
2. 查看用户列表
3. 点击「创建用户」，填写用户名、密码、角色
4. 点击用户行的「删除」按钮可移除用户（不可删除自己）

### 5.8 修改密码

1. 点击右上角用户名
2. 在弹窗中输入旧密码和新密码
3. 点击「确认修改」

---

## 附录

### A. 项目 GitHub 地址

**仓库链接**：https://github.com/zhy1057/2026-

### B. 技术参考

- HNSWLIB 官方文档：https://github.com/nmslib/hnswlib
- scanpy 官方文档：https://scanpy.readthedocs.io
- Vue 3 官方文档：https://vuejs.org
- Flask 官方文档：https://flask.palletsprojects.com
- ECharts 官方文档：https://echarts.apache.org

### C. 数据集说明

本项目使用的人体肝脏单细胞数据集（liver.h5ad）：
- 细胞数：69,032
- 基因数：32,397
- PCA 维度：30
- 细胞类型：19 种
- 包含 UMAP 和 t-SNE 嵌入坐标
